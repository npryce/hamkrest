buildscript {
    repositories {
        mavenCentral()
        maven { url "https://plugins.gradle.org/m2/" }
    }
}

plugins {
    id "org.jetbrains.kotlin.multiplatform" version "1.4.10"
    id "maven-publish"
    id "signing"
}

repositories {
    mavenCentral()
}

group = "com.natpryce.hamkrest"
version(hasProperty("-version") ? property("-version") : "SNAPSHOT")

println "building version ${version}"


kotlin {
    sourceSets {
        all {
            languageSettings {
                languageVersion = '1.4'
                apiVersion = '1.4'
            }
        }

        commonMain {
            dependencies {
                implementation kotlin('stdlib-common')
            }
        }

        commonTest {
            dependencies {
                implementation kotlin('test-common')
                implementation kotlin('test-annotations-common')
            }
        }
    }
}

apply from: "gradle/build-jvm.gradle"
apply from: "gradle/build-js.gradle"
apply from: "gradle/build-native.gradle"
apply from: "gradle/publishing.gradle"

